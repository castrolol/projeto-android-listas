<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.castrolol.aula.aprendendolista">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_bomboneria"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_bomboneria"
        android:supportsRtl="true"
        android:theme="@style/Theme.AprendendoLista">
        <activity android:name=".telas.NovaTela"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>